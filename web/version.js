// ── App version ───────────────────────────────────────────────────────────────
// Bump this string on every deployment.
// It is loaded by sw.js (via importScripts) to form the cache key, so changing
// it here automatically invalidates the old service-worker cache for all users.
// It is also loaded by index.html so the running app can read window.APP_VERSION.
// ─────────────────────────────────────────────────────────────────────────────
var APP_VERSION = '1.0.0'; // eslint-disable-line no-var

